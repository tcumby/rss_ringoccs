
execute_process(
    COMMAND
        conan install --conf tools.build:download_source=True --build=missing --deploy=full_deploy --output-folder
        ${CONAN_DEPLOY_DIR} ${CONAN_SOURCE_DIR}
    WORKING_DIRECTORY ${CONAN_SOURCE_DIR}
    COMMAND_ECHO STDOUT
    COMMAND_ERROR_IS_FATAL ANY
)
